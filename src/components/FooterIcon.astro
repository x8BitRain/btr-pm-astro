---
import { Image } from "astro:assets";
const { icon, alt } = Astro.props;
const icons = import.meta.glob("../data/images/icons/*.svg");
const res = icons[icon]();
---

<Image
  src={res}
  height="24"
  width="24"
  alt={alt}
  loading="eager"
  format="svg"
/>
